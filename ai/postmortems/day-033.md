# Day 033 Postmortem — Uchiwa Fan Dye Maestro

## QA summary

Day 033 was generated from the archived prompt at `prompts/day-033.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. The subagent produced partial Day 033 assets/scaffold but did not return a final summary in time for the controller workflow. The controller recovered by taking ownership of the Day 033 file set, completing the game logic, mobile CSS, immutable release, public `/uchiwa/` alias, semantic prompt pages, metadata, route smoke, screenshots, and validation.

A separate independent QA/review subagent was dispatched after controller recovery. The controller route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence unless QA reports a blocker.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 033 planning, implementation recovery, QA, or validation work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-033.md` was written. Day 033 HTML/CSS/JS was written for `apps/day-033-uchiwa-fan-dye-maestro/` from the archived Day 033 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/033/` and `/uchiwa/`.

## What shipped

- Public route: `/uchiwa/`.
- Immutable archive: `release/games/033/`.
- Mode: mobile-first `2d`, chosen after Day 032 `3d` with zero latest 2D streak.
- Core play: select radial fan sectors/bands, rotate stencil motifs, choose indigo/coral/saffron pigment, brush target sectors, fold paper edges, blot bleed, fan-dry clean lines, preserve white gaps, and activate Kaze-Dry Focus for visible bleed/dry/stencil previews.
- Mobile layout: portrait-first 390x844 route with compact topbar/HUD/commission, 284px fan stage, status helper, and all primary controls visible in the first viewport at 44px+.
- Prompt archive: `prompts/day-033.md` copied exactly to `release/games/033/prompt.md` and `release/uchiwa/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio paper tick, brush, stencil, fold, blot, dry, danger, focus, and Grand Breeze cues initialize only after Start/user gesture; visual stencil/bleed/dry-gap cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `uchiwa-helper-source.png` / `uchiwa-helper.png`: friendly upright kappa fanmaker in a blue festival happi coat, holding a blank fan and pigment brush. Clean pale washi background, no fake checkerboard, no readable text or watermark, centered crop, readable face/fan/brush silhouette, stable upright helper orientation. It is not true transparent alpha, but the soft background integrates cleanly in helper cards/badges.
- `uchiwa-workshop-source.png` / `uchiwa-workshop.png`: portrait-safe uchiwa workshop with washi sheets, fan ribs, pigment bowls, stencil sheets, charms, and a broad open central work surface. No people, readable text, or watermark. The central playable area remains readable behind the radial fan board on desktop and mobile.
- `uchiwa-icons-source.png` / `uchiwa-icons.png`: readable icon sheet with blank fan, bamboo rib fan, indigo/coral/saffron pigment drops, wave/goldfish/lantern/firefly stencils, blot cloth, folded fan edge, drying breeze, bleed warning, paper heart, Kaze-Dry Focus fan emblem, and Grand Breeze festival seal. No fake checkerboard, watermark, text, or edge crop; icons are distinct enough at HUD/button size while controls remain text-labeled.
- `screenshot.png`: final desktop 1280x900 route capture shows active gameplay with score/bleed/combo changes, Kaze-Dry Focus overlay, dyed fan sectors, helper art, commission card, and readable fan stage; it is suitable for the gallery card. Separate 390x844 mobile screenshot confirmed first-viewport usability.

## Fixes made during QA/recovery

- Recovered from partial implementation output by choosing controller-owned Day 033 game logic and synchronizing app/archive/alias copies.
- Generated/optimized Imagegen2 helper, background, and icon assets; archived source files under `release/games/033/assets/source/` and copied optimized playable assets to app, immutable release, and public alias.
- Rendered `prompt.html` with semantic headings/lists/code, title/h1, and back-to-game link; static/browser probes confirmed no raw Markdown `<pre>` dump, malformed list nodes, or `<strong>` inside `<code>`.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Tuned mobile CSS after the first route smoke found controls below the first viewport. Final mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`, stage height `284px`, minimum target height `44px`, controls bottom `825.5px`.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day033-smoke/`.

## Validation run

Full local validation completed before commit/push:

- `pnpm typecheck` — passed (`tsc --noEmit`).
- `pnpm test` — passed (`tests/release.test.ts`, 4 tests).
- `pnpm build` — passed (`vite build && node scripts/ensure-release-structure.mjs`), produced `release/index.html`, `release/assets/index-DFXTTwkp.css`, and `release/assets/index-Bw0yBNrw.js`.
- `pnpm release:validate` — passed (`release validation passed`).
- `pnpm test:smoke` — passed (4 Playwright gallery smoke tests across desktop/mobile Chromium).
- `pnpm test:immutable -- --base origin/main` — passed (`immutable guard passed`).
- Custom route smoke `/tmp/day033-route-smoke.mjs` — passed for `/uchiwa/` and `/uchiwa/prompt.html` on desktop 1280x900 and mobile 390x844, including menu/tutorial/start/gameplay/pause/restart, primary controls, prompt DOM, no console/page/request errors, and mobile metrics (`bodyScrollHeight=844`, `innerHeight=844`, min control height `44`, controls bottom `825.5`, stage height `284`).
- Docker/static smoke — passed after building `100-days-games:day033-smoke`, serving with Nginx on local port 18036, checking `/uchiwa/`, `/uchiwa/prompt.html`, and `nginx -t` inside the container.

## Remaining notes

- The route smoke exercises primary controls and visible feedback, but does not grind long enough to naturally trigger Uchiwa Grand Breeze. The progression path is implemented through authored commission targets, score gates, bleed/hearts state, chapter advancement, and Grand Breeze banner logic.
- The helper asset uses a clean pale background rather than true transparent alpha. It is acceptable for this day because it is shown inside rounded cards/badges and was inspected at final size.
