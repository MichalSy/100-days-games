# Day 030 Postmortem — Mikan Sunwheel Orchard

## QA summary

Day 030 was generated from the archived prompt at `prompts/day-030.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. That subagent began writing Day 030 scaffolding, but the controller observed conflicting/partial files and recovered by taking ownership of the Day 030 file set, generating Imagegen2 assets, completing the app/release/public alias, rendering prompt pages, updating metadata, and running controller-owned QA.

A separate read-only QA/review subagent was dispatched after controller recovery. The controller route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 030 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-030.md` was written. Day 030 HTML/CSS/JS was written for `apps/day-030-mikan-sunwheel-orchard/` from the archived Day 030 prompt and general browser/Three.js/WebAudio game knowledge, then copied into `release/games/030/` and `/mikan/`.

## What shipped

- Public route: `/mikan/`.
- Immutable archive: `release/games/030/`.
- Mode: mobile-first real `3d`, chosen after Day 029 `2d` with a latest 2D streak of one.
- Core play: orbit and raise/lower a bamboo basket around a 3D mikan canopy, pluck fruit by ripeness, catch/sort drops into crate lanes, rotate the Sunwheel, use Fan Gust against hornets, cushion drops with Leaf Net, and trigger Harvest Focus for slow/preview behavior.
- Mobile layout: portrait-first 390x844 route with compact HUD, commission card, 286px 3D stage, helper/status row, and all primary controls visible in the first viewport at 44px+.
- Prompt archive: `prompts/day-030.md` copied exactly to `release/games/030/prompt.md` and `release/mikan/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio leaf, pluck, basket, crate, sunwheel, hornet, net, focus, and Grand Harvest cues initialize only after Start/interaction; visual ripeness/drop/crate cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `mikan-helper-source.png` / `mikan-helper.png`: friendly tan shiba with indigo scarf, mikan, and bamboo basket. The asset has a clean pale citrus background, no fake checkerboard, no visible text or watermark, centered crop, readable face/basket/mikan silhouette, and stable upright helper orientation. It is not true transparent alpha, but the warm background integrates cleanly with helper cards and badges.
- `mikan-orchard-source.png` / `mikan-orchard.png`: portrait-safe sunny mikan orchard with citrus branches around edges, ladders/crates/sun reflector, open central path/canopy space, mountain haze, and no characters/readable text/watermark. The center stays readable behind the 3D stage because the game overlays a bright stage panel and procedural canopy.
- `mikan-icons-source.png` / `mikan-icons.png`: readable icon sheet with green mikan, golden ripe mikan, deep orange mikan, basket, crates, sunwheel, hornet, leaf net, wind ribbon, focus emblem, bruised fruit/meter, harvest heart, and Grand Harvest seal. No fake checkerboard, no watermark/text, no edge crop issue; icons remain distinct enough for HUD/button support while controls remain text-labeled.
- `screenshot.png`: final desktop route capture shows active gameplay with HUD, commission card, 3D mikan tree, basket/crates, helper badge, and large controls; it is readable as a sunny orchard harvest game and suitable for the gallery card. Separate 390x844 mobile screenshot confirmed first-viewport usability.

## Fixes made during QA/recovery

- Recovered from concurrent partial subagent output by choosing controller-owned Day 030 files, removing an unnecessary transient `scripts/prepare-day030.mjs`, and using minified Three.js plus matching `three.core.min.js` in app/archive/alias.
- Regenerated `prompt.html` with flat semantic lists after an initial renderer produced direct nested `ul > ul` / `ul > ol` nodes.
- Tightened mobile CSS so all controls fit in the first 390x844 viewport: topbar reduced to two rows, 4-column control grid, 44px+ touch targets, and a 286px stage.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day030-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-BZo-BCfE.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 030 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/mikan/`: menu/tutorial, Start/gameplay, Orbit Left/Right, Raise/Lower, Pluck/Catch, Crate Lane, Sunwheel, Fan Gust, Leaf Net, Harvest Focus, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics: `bodyScrollHeight=793`, `innerHeight=844`, stage height 286px, controls bottom 797.9px, minimum target height 44px.
- Static checks — passed for `release/games/030/`, `/mikan/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, vendored Three.js files, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-030.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 030 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw Markdown `<pre>` dump, and no malformed direct `ul/ol` child nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission/stage/status/controls.
- Docker/Nginx static smoke — passed with local image `100-days-games:day030-smoke`: `nginx -t` succeeded; `/mikan/`, `/mikan/prompt.html`, and `/manifest.json` served correctly on port 18031 with `completedDays=30`. The first readiness probe saw one transient connection reset before Nginx was ready, then retry/static checks passed.

## Remaining notes

- The implementation subagent did not provide the final authoritative implementation; controller recovery is documented here.
- The custom route smoke exercises primary controls and visible feedback, but does not grind long enough to naturally trigger Mikan Grand Harvest. The progression path is implemented through authored commission targets, score thresholds, bruise/hearts state, and chapter advancement.
