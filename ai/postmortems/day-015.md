# Day 015 Postmortem — Midori Bamboo Canal Keeper

## QA summary

Day 015 was generated from the archived prompt at `prompts/day-015.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. The controller also generated/inspected Imagegen2 assets, assembled/reconciled the Day 015 release, and completed the end-to-end validation because the run needed local browser/mobile smoke, screenshot capture, Docker/static smoke, and deployment readiness in the controller session. A separate fresh QA/review subagent was also dispatched after the local release was assembled.

The Hermes browser tool could not launch `/snap/bin/chromium`, so route QA used the repo's Playwright/browser stack with `/usr/bin/google-chrome` as the Chromium executable, following the documented fallback path.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 015 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-015.md` was written. Day 015 HTML/CSS/JS was written for `apps/day-015-midori-bamboo-canal-keeper/` from the archived Day 015 prompt and general browser/canvas game knowledge, then copied into `release/games/015/` and `/midori/`.

## What shipped

- Public route: `/midori/`.
- Immutable archive: `release/games/015/`.
- Mode: mobile-first `2d`, chosen deliberately after two `hybrid` days and zero current 2D streak.
- Core play: tap bamboo canal tiles to rotate openings, preview water flow, lock key pieces, pulse spring water, fill requested basins/lotus bowls, collect koi beads, use Moss Patch when charged, and manage drought/overflow/seedling hearts toward Midori Full-Grove Bloom.
- Mobile layout: portrait-first 390x844 route with compact gardener HUD, commission card, central canal board, visible helper/charge meter, and 44px+ Lock / Pulse Water / Moss Patch / Pause / Restart controls.
- Prompt archive: `prompts/day-015.md` copied exactly to `release/games/015/prompt.md` and `release/midori/prompt.md`; rendered semantic prompt pages at both aliases.

## Generated-image inspection notes

- `midori-keeper-source.png` / `midori-keeper.png`: readable upright tanuki canal keeper in moss-green clothing with straw hat, bamboo scoop, seedling, and koi-orange charm. Centered crop, stable pivot, strong small-size silhouette, no visible text or watermark. It has a pale green background rather than true alpha transparency; acceptable because it is menu/helper art, not a moving collision sprite.
- `midori-grove-source.png` / `midori-grove.png`: portrait bamboo grove background with open mossy center, bamboo canals and basin props on the sides, warm sunlight, no characters, no visible text or watermark. Crop-safe for phone portrait; the game overlays a high-contrast board panel so canal tiles, route preview, HUD, commission card, and controls remain readable.
- `midori-icons-source.png` / `midori-icons.png`: distinct straight/elbow/T/cross bamboo canals, spring source, moss basin, lotus bowl, blue/green/orange beads, sun-crack hazard, tanuki patch, seedling heart, and bloom seal. Generous margins, no visible text, watermark, or fake checkerboard. Runtime currently draws most gameplay symbols procedurally on the board, with this sheet available for decorative/UI use.
- `screenshot.png`: final desktop route capture shows non-empty gameplay with readable HUD, commission card, board, water preview, canal pieces, basins, tanuki helper, Moss charge, and bottom controls. Separate 390x844 mobile screenshot confirmed portrait usability and visible controls.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized/copied playable assets into app, immutable release, and `/midori/` alias paths; source assets are archived only under `release/games/015/assets/source/`.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Captured a desktop gameplay screenshot for gallery use and a separate mobile QA screenshot at `/tmp/day015-mobile.png`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 015 route smoke — passed for desktop 1280x900 and mobile 390x844: menu/tutorial, start/gameplay, canal board rendering, tile selection/rotation, Lock, Pulse Water, route preview, Moss Patch control presence, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Static checks — passed for `release/games/015/`, `/midori/`, screenshots, prompt copies, optimized assets, source assets, and non-empty game files.
- Prompt copy checks — passed: `prompts/day-015.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 015 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no body-level raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Docker/Nginx static smoke — passed for `/midori/`, `/midori/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day015-smoke`.

## Remaining notes

- The route smoke exercises primary controls and verifies the 2D water-routing board, but does not grind long enough to trigger Midori Full-Grove Bloom naturally. The progression path is implemented through authored chapter goals and score/request thresholds.
- The generated mascot is not a transparent cutout. This is acceptable for framed menu/helper usage, but future prompts should continue asking for true transparent PNG when assets are directly composited as moving sprites.
