# Day 022 Postmortem — Hoshi Nebuta Kite Cartographer

## QA summary

Day 022 was generated from the archived prompt at `prompts/day-022.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and the code-isolation rule. During implementation, the controller also generated and inspected Imagegen2 assets, then reconciled concurrent Day 022 writes by choosing the final Day 022 app/release/alias files, fixing the DOM/JS integration, and synchronizing the chosen files into `apps/day-022-hoshi-nebuta-kite-cartographer/`, `release/games/022/`, and `/hoshi/`.

A separate QA/review subagent was dispatched after implementation. The controller route smoke and full local validation are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 022 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-022.md` was written. Day 022 HTML/CSS/JS was written for `apps/day-022-hoshi-nebuta-kite-cartographer/` from the archived Day 022 prompt and general browser/Three.js game knowledge, then copied into `release/games/022/` and `/hoshi/`.

## What shipped

- Public route: `/hoshi/`.
- Immutable archive: `release/games/022/`.
- Mode: mobile-first real `3d`, chosen after Day 021 `hybrid` and zero current 2D streak.
- Core play: steer a glowing nebuta-paper kite through 3D night-sky depth, collect ordered star-ink nodes, manage thread tension with Reel In/Out, place Star Thread markers, reveal routes with Kitsune Gust, avoid cloud tangles, and bank constellation strokes at shrine beacons.
- Mobile layout: portrait-first 390x844 route with compact HUD, sky-map card, central 3D playfield, helper/status text, mascot art, and all primary controls fitting in the first viewport.
- Prompt archive: `prompts/day-022.md` copied exactly to `release/games/022/prompt.md` and `release/hoshi/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio paper flutter/thread/star/beacon/cloud/gust cues initialize only after Start or another user gesture; visual wind/tension cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `hoshi-kite-source.png` / `hoshi-kite.png`: luminous fox-shaped nebuta kite with a tiny kitsune pilot, braided red-white tail, and star tassels. It has a clean composited background/runtime crop, no visible text/watermark/checkerboard, strong small-size silhouette, centered pivot, and a stable forward/upright orientation suitable for a flying game piece.
- `hoshi-sky-source.png` / `hoshi-sky.png`: portrait night shrine-sky background with cobalt/violet sky, side cloud shelves, constellations, wind ribbons, and shrine lights below. No visible text or watermark. The central area remains open and readable under desktop and 390x844 mobile overlays.
- `hoshi-icons-source.png` / `hoshi-icons.png`: readable icon sheet with gold/blue stars, shrine beacon, braided thread marker, tension/reel icons, tail pulse/kite, Kitsune Gust seal, cloud tangle, wind arrow, kite-heart lantern, and Sky Map emblem. No fake checkerboard, visible text, watermark, or problematic crop after optimization/compositing. Runtime controls remain text-labeled, using the sheet as visual support.
- `screenshot.png`: final desktop route capture shows active 3D gameplay with readable HUD, mission card, sky playfield, kite, stars, helper text, mascot, and controls. Separate 390x844 mobile screenshot confirmed portrait usability and all primary controls fitting without scroll.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized/composited generated images onto stable night-blue backgrounds to avoid alpha/checkerboard ambiguity.
- Copied playable assets into app, immutable release, and `/hoshi/` alias paths; source assets are archived only under `release/games/022/assets/source/`.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Reconciled concurrent subagent/controller writes by matching Day 022 HTML IDs/data attributes to the final Three.js game module and copying the final app files back to both served release locations.
- Compressed mobile layout after screenshot review so the 390x844 first viewport contains the HUD, sky-map card, 3D playfield, helper, and all primary controls.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day022-smoke/`.

## Validation run

- Baseline `pnpm typecheck` — passed.
- Baseline `pnpm test` — passed: 1 test file / 4 tests.
- Custom Day 022 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, start/gameplay, Steer Up/Left/Right/Down, Tail Pulse, Reel Out/In, Star Thread, Kitsune Gust, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`.
- Static checks — passed for `release/games/022/`, `/hoshi/`, screenshots, prompt copies, optimized assets, source assets, vendored Three.js files, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-022.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed in custom smoke and static check: `Day 022 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, readable, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/mission/playfield and tappable controls.
- Final `pnpm typecheck` — passed.
- Final `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Docker/Nginx static smoke — passed for `/hoshi/`, `/hoshi/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day022-smoke`.

## Remaining notes

- The custom route smoke exercises primary controls and verifies the 3D sky route, but does not grind long enough to naturally trigger Hoshi Sky Map Complete. The progression path is implemented through authored chapter requirements, ordered stars, marker counts, beacon banking, tension state, and score thresholds.
- The implementation subagent produced useful Day 022 work concurrently with controller fixes; the controller intentionally selected the final file owner and synchronized app/release/alias files before validation.
