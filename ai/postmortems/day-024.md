# Day 024 Postmortem — Usagi Mochi Moon Hopper

## QA summary

Day 024 was generated from the archived prompt at `prompts/day-024.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. While that subagent began creating Day 024 files, the controller detected concurrent writes, intentionally chose the controller-owned implementation as the final file owner, and synchronized the final Day 024 app files into `apps/day-024-usagi-mochi-moon-hopper/`, `release/games/024/`, and `/usagi/` before validation.

A separate QA/review subagent was dispatched after implementation. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 024 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-024.md` was written. Day 024 HTML/CSS/JS was written for `apps/day-024-usagi-mochi-moon-hopper/` from the archived Day 024 prompt and general browser/Three.js/WebAudio game knowledge, then copied into `release/games/024/` and `/usagi/`.

## What shipped

- Public route: `/usagi/`.
- Immutable archive: `release/games/024/`.
- Mode: mobile-first real `3d`, chosen after Day 023 `2d` with only a one-game 2D streak.
- Core play: aim and charge a moon-rabbit hop across 3D mochi pads, land cleanly for combo, collect ordered rice sparks, stabilize pads with Knead/Brace, drop Rice Markers, dash through tray gates, use Moon Whisker Focus, avoid soot-bat shadows, and deliver to moon trays.
- Mobile layout: portrait-first 390x844 route with compact HUD, delivery card, 3D playfield, helper/status text, and 45px+ controls all inside the first viewport (`bodyScrollHeight=844`, `innerHeight=844`).
- Prompt archive: `prompts/day-024.md` copied exactly to `release/games/024/prompt.md` and `release/usagi/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio boing/crack/spark/mallet/dash/focus/feast cues initialize only after Start or another user gesture; visual hop/landing cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `usagi-hopper-source.png` / `usagi-hopper.png`: friendly white usagi mascot holding a mochi mallet and rice spark, no text/watermark/checkerboard, strong small-size silhouette, centered crop, energetic upright hopping pose. The generated source uses a clean pale lavender background rather than reliable true alpha; an aggressive alpha-clean attempt damaged the ears/silhouette, so the source/runtime asset was kept as a solid-background portrait asset while the in-game player uses a clean 3D usagi model to avoid a square sprite artifact.
- `usagi-rooftops-source.png` / `usagi-rooftops.png`: portrait moonlit rooftop mochi festival with open central play area, full moon, lantern rooftops, red trays, paper charms, and jade/cream mochi pads at the edges. No characters, readable text, or watermark. Desktop and 390x844 mobile overlays remain readable.
- `usagi-icons-source.png` / `usagi-icons.png`: readable icon sheet with mochi pad types, rice sparks, tray gate, moon tray, moon-heart, cracked pad, soot-bat hazard, wind charm, hop actions, mallet, marker grain, focus emblem, and feast emblem. No fake checkerboard, visible text, watermark, or problematic edge crop. Runtime controls are text-labeled, using the sheet as visual support/decorative asset.
- `screenshot.png`: final desktop route capture shows active 3D gameplay with readable HUD, mission card, course depth, mochi pads, tray gate, helper text, and controls. Separate 390x844 mobile screenshot confirmed first-viewport usability and all controls within the viewport.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized generated assets; archived sources only under `release/games/024/assets/source/` and copied playable assets into app, immutable release, and `/usagi/` alias paths.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Reconciled controller/subagent concurrent writes by selecting the controller-owned app files and copying them to both served release locations.
- Fixed mobile layout after route smoke caught first-viewport scrolling; final mobile metrics are `bodyScrollHeight=844`, `innerHeight=844`, controls 45px high.
- Fixed desktop screenshot capture/layout after gallery screenshot review showed top HUD cropping; final screenshot has intact HUD and controls.
- Removed the generated mascot as a square in-game billboard and replaced it with a clean 3D usagi player piece so the gameplay screenshot has no square sprite artifact.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 024 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, start/gameplay, aim controls, Charge Hop, Short Hop, Moon Dash control presence, Knead/Brace, Rice Marker, Moon Whisker Focus, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`, controls 45px high.
- Static checks — passed for `release/games/024/`, `/usagi/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-024.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 024 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed after the layout/player fixes: desktop screenshot is non-empty, active, readable, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/mission/stage/helper/controls.
- Docker/Nginx static smoke — passed for `/usagi/`, `/usagi/prompt.html`, `/manifest.json` (`completedDays=24`), and `nginx -t` inside local image `100-days-games:day024-smoke`.

## Remaining notes

- The custom route smoke exercises primary controls and verifies 3D route interaction, but does not grind long enough to naturally trigger Usagi Moon Feast. The progression path is implemented through authored delivery requirements, ordered sparks, clean landings, pad stability, tray delivery, chapter advancement, and score thresholds.
- The initial implementation subagent produced concurrent Day 024 app files while the controller was recovering. The controller intentionally selected one final file owner and synchronized app/release/alias files before validation.
