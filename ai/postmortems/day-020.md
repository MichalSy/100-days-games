# Day 020 Postmortem — Umi Pearl Kelp Cartographer

## QA summary

Day 020 was generated from the archived prompt at `prompts/day-020.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. The controller also generated and inspected Imagegen2 assets, reconciled the final app/release/alias output from the archived Day 020 prompt, and ran local browser/mobile smoke, screenshot capture, static checks, and validation.

A concurrent implementation agent began creating Day 020 files while the controller was preparing recovery work, so the controller intentionally chose the final file owner for Day 020 app/release/alias files, re-read the current Day 020 paths, and synchronized the chosen app files back into both `release/games/020/` and `/umi/` before smoke testing. No existing release folders were modified.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 020 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-020.md` was written. Day 020 HTML/CSS/JS was written for `apps/day-020-umi-pearl-kelp-cartographer/` from the archived Day 020 prompt and general browser/Three.js game knowledge, then copied into `release/games/020/` and `/umi/`.

## What shipped

- Public route: `/umi/`.
- Immutable archive: `release/games/020/`.
- Mode: mobile-first real `3d`, chosen after Day 019 `2d` and a latest 2D streak of one.
- Core play: steer a tiny ama-diver through a 3D underwater kelp canyon, collect requested pearls in order, place Guide Shell markers in calm eddies, refill oxygen at Air Bells, dodge moon jellyfish, ride/avoid current ribbons, charge Sonar Bloom, and complete the Umi Pearl Atlas.
- Mobile layout: portrait-first 390x844 route with compact underwater HUD, dive-map card, central 3D canyon, oxygen ring, status helper, mascot, and large 52px+ steering/action controls.
- Prompt archive: `prompts/day-020.md` copied exactly to `release/games/020/prompt.md` and `release/umi/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio pearl/bell/current/jellyfish/sonar/atlas cues initialize only after Start or another user gesture; visual oxygen/sonar cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `umi-diver-source.png` / `umi-diver.png`: friendly ama-diver/sea-spirit mascot with shell compass, pearl lantern satchel, indigo swim wrap, and bubble trail. Centered upright crop, generous margins, strong small-size silhouette, no visible text or watermark. It uses a pale aqua background rather than true alpha; acceptable because it is helper/menu art and the runtime 3D diver uses a readable sphere/sprite combination. Pose does not imply a wrong forward direction.
- `umi-kelp-cove-source.png` / `umi-kelp-cove.png`: portrait underwater kelp cove with coral torii/shrine buoys, teal fog, sun rays, bubbles, and open central corridor. No visible text, characters, or watermark. The center remains readable after desktop and 390x844 mobile overlays, while side kelp and lower coral details provide depth without covering active objects.
- `umi-icons-source.png` / `umi-icons.png`: generated sheet contains distinct moon/tide/gold pearl, guide shell, oxygen ring, air bell, current arrow, jellyfish, kelp gate, coral torii buoy, sonar seal, shell-heart, and atlas emblem. The source contained a fake checkerboard-style transparent background; the optimized runtime copies were flood-cleaned/composited onto a pale aqua background before shipping. Critical controls remain text-labeled, using the sheet as decorative/UI reference.
- `screenshot.png`: final desktop route capture shows non-empty gameplay with readable HUD, dive-map card, 3D canyon, pearls, kelp, oxygen ring, helper, mascot, and large controls. Separate 390x844 mobile screenshot confirmed portrait usability and visible controls.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized/copied playable assets into app, immutable release, and `/umi/` alias paths; source assets are archived only under `release/games/020/assets/source/`.
- Cleaned the optimized icon sheet to remove the baked checkerboard-style transparency background while preserving distinct icons.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Vendored `three.module.min.js` and the matching `three.core.min.js` into app, immutable release, and `/umi/` alias asset folders.
- Captured a desktop gameplay screenshot for gallery use and a separate mobile QA screenshot at `/tmp/day020-mobile-gameplay.png`.

## Validation run

- Baseline `pnpm typecheck` — passed.
- Baseline `pnpm test` — passed: 1 test file / 4 tests.
- Final `pnpm typecheck` — passed.
- Final `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 020 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, start/gameplay, Steer Up/Left/Right/Down, Pulse, Brake/Drift, Guide Shell, Sonar Bloom control presence, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Static checks — passed for `release/games/020/`, `/umi/`, screenshots, prompt copies, optimized assets, source assets, vendored Three.js files, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-020.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 020 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, readable, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/dive card/playfield and tappable controls.

## Remaining notes

- The route smoke exercises primary controls and verifies the 3D underwater route, but does not grind long enough to naturally trigger Umi Pearl Atlas. The progression path is implemented through authored dive-map requirements, score thresholds, pearl order, guide shell counts, and air-bell refills.
- The icon sheet source had baked checkerboard transparency, reinforcing the existing generator lesson: keep explicit “solid pale background, no checkerboard” wording for icon sheets and inspect optimized assets after cleanup.
