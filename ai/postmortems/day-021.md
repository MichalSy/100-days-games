# Day 021 Postmortem — Aki Karesansui Ripplekeeper

## QA summary

Day 021 was generated from the archived prompt at `prompts/day-021.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. The controller also generated and inspected Imagegen2 assets, then recovered/owned the final Day 021 app/release/alias files after noticing concurrent Day 021 writes. The chosen final files were synchronized into `apps/day-021-aki-karesansui-ripplekeeper/`, `release/games/021/`, and `/aki/`, then local browser/mobile smoke, screenshot capture, static checks, Docker/static smoke, and the required validation suite were run.

A separate QA/review subagent was dispatched after implementation for an independent review pass. The controller did not wait to publish until a hypothetical external CI/browser runner; the local Playwright route smoke and full validation suite below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 021 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-021.md` was written. Day 021 HTML/CSS/JS was written for `apps/day-021-aki-karesansui-ripplekeeper/` from the archived Day 021 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/021/` and `/aki/`.

## What shipped

- Public route: `/aki/`.
- Immutable archive: `release/games/021/`.
- Mode: mobile-first meaningful `hybrid`, chosen after Day 020 `3d` and zero current 2D streak.
- Core play: rake diagonal sand-ripple strokes, rotate standing stones, release red/gold/white autumn leaves, guide them into moon basins, avoid moss/no-rake zones, manage turbulence/harmony, use Tamp Sand, Basin Bell, and Still Garden.
- Mobile layout: portrait-first 390x844 route with compact autumn HUD, commission card, central dry-garden board, status helper, tanuki mascot, and 52px+ rake/action controls.
- Prompt archive: `prompts/day-021.md` copied exactly to `release/games/021/prompt.md` and `release/aki/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio rake/stone/leaf/basin/moss/stillness cues initialize only after Start or another user gesture; visual route previews remain playable if muted/unsupported.

## Generated-image inspection notes

- `aki-tanuki-source.png` / `aki-tanuki.png`: friendly tanuki garden keeper with bamboo rake, amber maple leaf, indigo apron, and straw hat. Centered crop, generous margins, strong small-size silhouette, no visible text/watermark, no checkerboard. It uses a clean pale sand background rather than true alpha; acceptable because it is helper/menu art, not a collision sprite. Rake pose is upright and does not imply a wrong control direction.
- `aki-garden-source.png` / `aki-garden.png`: portrait karesansui dry garden with pale raked sand, moss islands at sides/corners, standing stones, bamboo fence, falling maple leaves, and bronze basin. No visible text, characters, or watermark. The center play area remains open and readable after desktop and 390x844 mobile overlays.
- `aki-icons-source.png` / `aki-icons.png`: readable icon sheet with red/gold/white leaves, standing/crane stones, moss, basin, rake, tamp stamp, bell, Still Garden seal, harmony heart, turbulence swirl, and Golden Stillness emblem. No fake checkerboard, visible text, watermark, or edge cropping after optimization. Runtime controls remain text-labeled, using the sheet as visual/decorative support.
- `screenshot.png`: final desktop route capture shows active gameplay with score, a delivered leaf, visible ripple strokes, dry-garden board, moss islands, basins, stones, helper text, tanuki mascot, and readable controls. Separate 390x844 mobile screenshot confirmed portrait usability and visible controls.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized/composited generated images onto pale sand backgrounds to avoid alpha/checkerboard ambiguity.
- Copied playable assets into app, immutable release, and `/aki/` alias paths; source assets are archived only under `release/games/021/assets/source/`.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Captured a desktop gameplay screenshot for gallery use and a separate mobile QA screenshot at `/tmp/day021-smoke/mobile-gameplay.png`.
- Adjusted desktop control grid after screenshot review so labels remain readable instead of crowding into a single row.

## Validation run

- Baseline `pnpm typecheck` — passed.
- Baseline `pnpm test` — passed: 1 test file / 4 tests.
- Final `pnpm typecheck` — passed.
- Final `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 021 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, start/gameplay, Rake directions, canvas stroke, Rotate Stone, Release Leaves, Tamp Sand, Basin Bell, Still Garden control presence, pause/resume, restart, prompt page, semantic prompt DOM, WebAudio running after Start, no console errors or request failures.
- Static checks — passed for `release/games/021/`, `/aki/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-021.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 021 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission card/playfield and tappable controls.
- Docker/Nginx static smoke — passed for `/aki/`, `/aki/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day021-smoke`.

## Remaining notes

- The custom route smoke exercises primary controls and verifies the hybrid sand-ripple route, but does not grind long enough to naturally trigger Aki Golden Stillness. The progression path is implemented through authored commission requirements, score thresholds, leaf deliveries, basin goals, turbulence/harmony state, and commission completion.
- The implementation subagent produced concurrent Day 021 writes but did not provide a usable summary before the controller finalized; the controller deliberately selected the final file owner and synchronized app/release/alias files before validation.
