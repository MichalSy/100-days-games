# Day 028 Postmortem — Akane Foxfire Shrine Sentinel

## QA summary

Day 028 was generated from the archived prompt at `prompts/day-028.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. The subagent produced Day 028 assets plus a partial app shell, then stopped making visible progress without a completion summary during the controller run. The controller recovered by adopting the new Day 028 partial files, writing the missing game logic/release/prompt/metadata files from the archived prompt, synchronizing `apps/day-028-akane-foxfire-shrine-sentinel/`, `release/games/028/`, and `/akane/`, and running independent QA/validation before publish.

A separate QA/review pass was dispatched after controller recovery. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 028 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-028.md` was written. Day 028 HTML/CSS/JS was written for `apps/day-028-akane-foxfire-shrine-sentinel/` from the archived Day 028 prompt and general browser/Three.js/WebAudio game knowledge, then copied into `release/games/028/` and `/akane/`.

## What shipped

- Public route: `/akane/`.
- Immutable archive: `release/games/028/`.
- Mode: mobile-first real `3d`, chosen after Day 027 `2d` with only a one-game 2D streak.
- Core play: step a kitsune sentinel through a 3D akane-dusk shrine, call/hold/release foxfire wisps, light ordered lanterns, hide in safe pools, avoid shadow-yokai cone sweeps, distract patrols with Suzu Bell, place Ema Charms, rotate camera, and use Moon Veil cone previews.
- Mobile layout: portrait-first 390x844 route with compact HUD, vigil card, 3D shrine stage, helper/status text, and 52px+ primary controls.
- Prompt archive: `prompts/day-028.md` copied exactly to `release/games/028/prompt.md` and `release/akane/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio footstep, wisp, lantern, shadow, suzu, ema, Moon Veil, and Grand Vigil cues initialize only after Start or another user gesture; visual wisp/cone/light cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `akane-helper-source.png` / `akane-helper.png`: friendly kitsune shrine sentinel holding a brass suzu bell and blue foxfire. The generated asset has a clean deep-violet shrine background, no fake checkerboard, no visible text or watermark, centered crop, strong readable silhouette, stable upright helper orientation, and works well for menu/helper art. It is not true transparent alpha, but the dark background integrates cleanly with the runtime cards.
- `akane-shrine-source.png` / `akane-shrine.png`: portrait akane-dusk Inari shrine corridor with torii gates, lanterns, cedar shadows, open central stone path, and strong vertical depth. It has no readable text or watermark. The central path remains readable behind the transparent Three.js stage and is crop-safe for portrait mobile.
- `akane-icons-source.png` / `akane-icons.png`: readable icon sheet with foxfire, lantern, torii, shadow-cone/eye, suzu bell, ema charm, spirit seal heart, Moon Veil crescent, alert/kitsune mask, safe light pool, route arrow, paw mark, and Grand Vigil seal. No fake checkerboard, watermark, text, or edge crop issue; icons are distinct enough for HUD/button support while controls remain text-labeled.

## Fixes made during QA/recovery

- Recovered from a partial implementation subagent run by adopting its new Day 028 assets/app shell and completing controller-owned `game.js`, release copies, prompt pages, metadata, screenshot, and postmortem.
- Optimized generated assets and archived sources only under `release/games/028/assets/source/`; copied playable assets into app, immutable release, and `/akane/` alias paths.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Vendored `three.module.min.js` with matching `three.core.min.js` in app, immutable release, and `/akane/` alias assets.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-2XJ25y_9.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 028 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/akane/`: menu/tutorial, Start/gameplay, Step Up/Left/Right/Down, Camera, Call/Hold Wisps, Release Wisps, Light Lantern, Suzu Bell, Ema Charm, Moon Veil, pause/resume, restart, prompt page, semantic prompt DOM, WebAudio after user gesture, no console errors or request failures. Mobile metrics: `bodyScrollHeight=852`, `innerHeight=844`, stage height 274.5px, controls 52px+ high, and max control bottom 844.6px (sub-pixel rounding only).
- Static checks — passed for `release/games/028/`, `/akane/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-028.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 028 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, no malformed `ul > p`, `ol > p`, direct nested list nodes, or corrupted inline code.
- Screenshot/vision QA — passed after recapturing the desktop gallery screenshot at scroll top and adding the generated shrine background into the stage: screenshot is active, readable, visually recognizable as a foxfire shrine sentinel game, and suitable for gallery use. Separate 390x844 mobile screenshot confirmed first-viewport usability.
- Docker/Nginx static smoke — passed with local image `100-days-games:day028-smoke`: `nginx -t` succeeded; `/akane/`, `/akane/prompt.html`, and `/manifest.json` served correctly on port 18028 with `completedDays=28`. The first readiness probe saw one transient connection reset before Nginx was ready, then retry/static checks passed.

## Remaining notes

- The implementation subagent did not return a completion summary during the controller run; controller recovery is documented here.
- The custom route smoke exercises all primary controls and verifies visible state changes, but does not grind long enough to naturally trigger Akane Grand Vigil. The progression path is implemented through authored vigil requirements, score gates, lantern order, wisp preservation, alert/seal state, and chapter advancement.
