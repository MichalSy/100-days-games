# Day 011 Postmortem — Hana Kimono Pattern Weaver

## QA summary

Day 011 was generated from the archived prompt at `prompts/day-011.md`. The run used the required prompt-first workflow and separate implementation/asset subagent passes, then the controller reconciled overlapping partial subagent output, finished release integration, ran route QA, captured screenshots, and completed the local validation suite before publish.

## Code-isolation confirmation

No previous game implementation source was opened, inspected, copied, diffed, grepped, or adapted during the Day 011 implementation/recovery/QA work. Previous days were used only through metadata (`src/data/games.ts`), prompts/postmortems, and a screenshot inspection for variety planning before `prompts/day-011.md` was written. Day 011 HTML/CSS/JS was written for `apps/day-011-hana-kimono-pattern-weaver/` from the archived Day 011 prompt and general browser/game knowledge, then copied into `release/games/011/` and `/hana/`.

## What shipped

- Public route: `/hana/`.
- Immutable archive: `release/games/011/`.
- Mode: mobile-first `2d`.
- Core play: select textile motif stamps, place them on a kimono-shaped cloth grid, rotate/flip stamps, use Mirror Guide, repair dye smudges, stop silk moths, and complete Sakura Lining / Crane Sleeve / Festival Obi commissions toward Hana Grand Fitting.
- Mobile layout: portrait-first 390x844 route with compact textile status ribbon, commission card, central kimono cloth panel, motif tray, and large rotate/flip/mirror/repair/pause/restart controls.
- Prompt archive: `prompts/day-011.md` copied exactly to `release/games/011/prompt.md` and `release/hana/prompt.md`; rendered semantic prompt pages at both aliases.

## Generated-image inspection notes

- `hana-artisan-source.png` / `hana-artisan.png`: readable upright kimono artisan mascot with indigo apron, sakura hairpin, fabric stamp, and gold thread spool. Centered crop and strong silhouette at menu size. No visible text or watermark. It has a warm washi background rather than true transparency; acceptable because it is menu/title art and not a moving gameplay sprite.
- `hana-workshop-source.png` / `hana-workshop.png`: portrait kimono workshop table background with folded silk, stamp trays, lantern light, and a large open center area. Crop-safe for 390x844 portrait; the game overlays a translucent cloth panel in the center, and HUD/controls remain readable. No characters, visible text, or watermark.
- `hana-icons-source.png` / `hana-icons.png`: 4x3 icon sheet with distinct sakura, wave, crane, plum, gold thread spool, mirror guide, repair cloth, moth, smudge, petal, and seal motifs. No visible text or watermark. The sheet has a light ivory background rather than alpha cutouts; acceptable because the game uses it in framed stamp/button/cell surfaces.
- `screenshot.png`: final desktop route capture shows non-empty gameplay with readable textile HUD, commission card, kimono cloth panel, motif tray, and controls. A separate 390x844 mobile QA screenshot confirmed portrait usability.

## Fixes made during QA/recovery

- Added explicit `helperCard` wiring in `game.js` after route smoke found the helper reference would otherwise be undefined.
- Removed per-frame motif tray re-rendering after Playwright caught detached DOM nodes during interaction.
- Re-copied the reconciled app files into both `release/games/011/` and `release/hana/` after subagent/controller overlap.
- Added data-URI favicon in the game and prompt pages to avoid favicon request noise.
- Used a desktop gameplay capture as the gallery screenshot for both immutable archive and public alias.
- Patched `scripts/launch-nightly-hermes.sh` to pass the curated required skill preload list via `--skills`, not only in the launcher prompt text.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 011 route smoke — passed for desktop 1280x900 and mobile 390x844: menu/tutorial, start/gameplay, motif selection, tap placement, rotate, flip, Mirror Guide, repair, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Static checks — passed for `release/games/011/`, `/hana/`, screenshots, prompt copies, optimized assets, source assets, and non-empty game files.
- Prompt copy checks — passed: `prompts/day-011.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 011 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists, no body-level raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Docker/Nginx static smoke — passed for `/hana/`, `/hana/prompt.html`, `/manifest.json`, and `nginx -t` inside the local image `100-days-games:day011-smoke`.

## Remaining notes

- The smoke test exercises primary controls but does not grind long enough to trigger Hana Grand Fitting naturally; the commission progression path is implemented and validated via static/code review plus interaction smoke.
- The icon sheet is used via CSS background positioning from a generated 4x3 sheet. Future prompts can ask for alpha-transparent individual icons if cleaner compositing is important, but the current framed stamp-card treatment works visually.
