# Day 027 Postmortem — Haru Matcha Whisk Atelier

## QA summary

Day 027 was generated from the archived prompt at `prompts/day-027.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. During the run, controller work and subagent/controller file creation overlapped; the controller intentionally reconciled the Day 027 files, selected one final app/release/alias implementation, and synchronized `apps/day-027-haru-matcha-whisk-atelier/`, `release/games/027/`, and `/haru/` before validation.

A separate independent QA/review subagent was dispatched after implementation. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 027 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-027.md` was written. Day 027 HTML/CSS/JS was written for `apps/day-027-haru-matcha-whisk-atelier/` from the archived Day 027 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/027/` and `/haru/`.

## What shipped

- Public route: `/haru/`.
- Immutable archive: `release/games/027/`.
- Mode: mobile-first `2d`, chosen after Day 026 `3d` with zero latest 2D streak.
- Core play: circular matcha whisking, orbit tightening/widening, powder/clump management, Rest Whisk, Steam Fan cooling, Tea Focus crest preview, Serve Bowl checks, temperature/bitterness pressure, three authored tea commissions, and Perfect Usucha finale.
- Mobile layout: portrait-first 390x844 route with compact header/HUD, commission card, large circular bowl stage, helper/status text, and 48px+ controls in the first viewport (`bodyScrollHeight=844`, `innerHeight=844`).
- Prompt archive: `prompts/day-027.md` copied exactly to `release/games/027/prompt.md` and `release/haru/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio whisk, powder, skim, fan, focus, serve/fail cues initialize only after Start or direct bowl gesture; visual foam/crest cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `haru-helper-source.png` / `haru-helper.png`: friendly tanuki tea helper holding a bamboo whisk and matcha bowl. The generated asset has a clean pale hinoki background, no fake checkerboard, no visible text or watermark, centered crop, strong readable silhouette, stable upright/bowing helper orientation, and works well in the helper card. It is not true transparent alpha, but the light background integrates cleanly with the tea-room UI.
- `haru-tearoom-source.png` / `haru-tearoom.png`: portrait tea-room/veranda background with sakura garden, shoji doors, tea tools around the edges, and a large open central tabletop area. It has no characters in the center, no readable text or watermark, and remains readable behind the bowl stage on desktop and mobile.
- `haru-icons-source.png` / `haru-icons.png`: readable icon sheet with chawan, chasen whisk, foam pearl, three-leaf crest, sakura petal, powder scoop, clump, steam fan, rest/focus-style emblems, temperature gauge, bitterness droplet, ceremony heart, serve tray, and Perfect Usucha seal. No fake checkerboard, watermark, text, or edge crop issue; icons are distinct enough for HUD/button support while controls remain text-labeled.
- `screenshot.png`: final desktop route capture shows active gameplay with header, HUD, commission card, large bowl canvas, helper card, and a clear spring matcha visual identity. Separate 390x844 mobile screenshot confirmed first-viewport usability and all primary controls visible.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual generated/optimized files with vision before publish.
- Optimized generated assets and archived sources only under `release/games/027/assets/source/`; copied playable assets into app, immutable release, and `/haru/` alias paths.
- Reconciled overlapping Day 027 implementation writes by selecting final controller-owned game logic and ensuring app/release/alias files match.
- Fixed initial JS/HTML mismatch from overlapping writes by aligning `game.js` with the final Day 027 DOM and re-running route smoke.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Fixed prompt-rendered nested-list DOM (`ul > ul`/`ol > ul`) by flattening Markdown list rendering, then re-ran prompt DOM checks.
- Tightened mobile CSS to fit the complete gameplay route into a 390x844 first viewport: body scroll height now equals viewport height, controls are 48px+ high, and the bowl stage remains 337px high.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day027-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-BGSoACAu.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 027 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/haru/`: menu/tutorial, start/gameplay, Whisk CW/CCW, Tighten/Widen Orbit, Matcha Powder, Skim Clump, Rest Whisk, Steam Fan, Serve, pause/resume, restart, prompt page, semantic prompt DOM, WebAudio after user gesture, no console errors or request failures. Mobile metrics after fix: `bodyScrollHeight=844`, `innerHeight=844`, stage height 337px, controls 48px+ high and bottom at 795px.
- Static checks — passed for `release/games/027/`, `/haru/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-027.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 027 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p`, `ol > p`, `ul > ul`, `ul > ol`, `ol > ul`, or `ol > ol` nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission/bowl/helper/controls.
- Docker/Nginx static smoke — passed with local image `100-days-games:day027-smoke`: `nginx -t` succeeded; `/haru/`, `/haru/prompt.html`, and `/manifest.json` served correctly on port 18029 with `completedDays=27`. The first readiness probe saw one transient connection reset before Nginx was ready, then the retry/static checks passed.

## Remaining notes

- The custom route smoke exercises primary controls and verifies bowl/foam/clump/temperature/serve interaction, but does not grind long enough to naturally trigger Haru Perfect Usucha. The progression path is implemented through authored commission requirements, score thresholds, serve checks, hearts/bitterness state, and chapter advancement.
- No generator prompt/rubric/template change was needed from this run; the existing mobile-first, asset-inspection, prompt-DOM, and audio QA rules caught the important issues.
