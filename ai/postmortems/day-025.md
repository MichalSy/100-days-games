# Day 025 Postmortem — Neko Koban Bell Cascade

## QA summary

Day 025 was generated from the archived prompt at `prompts/day-025.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. While that subagent began creating Day 025 files/assets, the controller detected partial concurrent Day 025 writes, intentionally chose the controller-owned implementation as the final file owner, and synchronized final app files into `apps/day-025-neko-koban-bell-cascade/`, `release/games/025/`, and `/neko/` before validation.

A separate QA/review subagent was dispatched after implementation. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 025 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-025.md` was written. Day 025 HTML/CSS/JS was written for `apps/day-025-neko-koban-bell-cascade/` from the archived Day 025 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/025/` and `/neko/`.

## What shipped

- Public route: `/neko/`.
- Immutable archive: `release/games/025/`.
- Mode: mobile-first `2d`, chosen after Day 024 `3d` with zero latest 2D streak.
- Core play: choose a coin drop slot, drop koban coins, rotate cat-paw bumpers, toggle charm gates, ring Fortune Bell, nudge the cabinet carefully, use Lucky Paw slow/path-preview focus, and bank requested coins into matching trays while managing misfortune/luck bells.
- Mobile layout: portrait-first 390x844 route with compact HUD, fortune card, tall pachinko board, helper/status text, and 52px controls all in the first viewport (`bodyScrollHeight=851`, `innerHeight=844`; only a 7px rounding overflow, no meaningful scroll needed).
- Prompt archive: `prompts/day-025.md` copied exactly to `release/games/025/prompt.md` and `release/neko/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio coin/bell/gate/nudge/focus cues initialize only after Start or another user gesture; visual coin paths remain playable if muted/unsupported.

## Generated-image inspection notes

- `neko-helper-source.png` / `neko-helper.png`: friendly maneki-neko helper holding a koban and fortune slip. Optimized asset is centered, high contrast, readable at small size, no checkerboard, no watermark, no unwanted readable text, and has a stable upright helper/menu orientation. It uses a clean warm cream background rather than true alpha, which is acceptable for helper/menu art.
- `neko-cabinet-source.png` / `neko-cabinet.png`: red-and-gold portrait lucky-cat pachinko cabinet with open central board, pegs, side cats, bells, lanterns, and coin trays. The generated optimized asset initially had a visible kanji-like inscription on the lower-right cat; the playable optimized copies were post-processed with a decorative koban medallion over that area and re-inspected. The center play area remains open/readable under desktop and 390x844 mobile overlays. Raw source remains archived as generated evidence; optimized playable copies in app/release/alias are the inspected/patched runtime assets.
- `neko-icons-source.png` / `neko-icons.png`: readable decorative icon sheet with gold/silver/copper coins, paw bumper, bell, trays, gate states, gutter, omikuji, Lucky Paw, luck bell, nudge arrows, and Grand Fortune emblem. No fake checkerboard, watermark, edge crop, or readable text issue after optimization. Runtime controls remain text-labeled, using the sheet as visual support/decorative asset.
- `screenshot.png`: final desktop route capture shows active gameplay with readable HUD, fortune card, pachinko board, selected drop slot, bumpers, gates, trays, helper text, and controls. Separate 390x844 mobile screenshot confirmed first-viewport usability and all controls visible.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized generated assets and archived sources only under `release/games/025/assets/source/`; copied playable assets into app, immutable release, and `/neko/` alias paths.
- Post-processed the cabinet runtime image to cover a readable generated inscription on a side cat with a decorative koban medallion, then re-inspected the optimized asset.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Reconciled controller/subagent concurrent writes by selecting the controller-owned app files and copying them to both served release locations.
- Increased mobile control targets from 45px to 52px after route-smoke metrics, then re-ran desktop/mobile route smoke.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day025-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-DD-Jh6zF.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 025 route smoke — passed for desktop 1280x900 and mobile 390x844 using Playwright Chromium against `/neko/`: menu/tutorial, start/gameplay, Slot Left/Right, Drop Coin, Rotate Paw, Toggle Gate, Nudge, pause/resume, restart/results restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics after fix: `bodyScrollHeight=851`, `innerHeight=844`, controls 52px high, bottom controls at 838px.
- Static checks — passed for `release/games/025/`, `/neko/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-025.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 025 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/fortune card/board/helper/controls.
- Docker/Nginx static smoke — passed for `/neko/`, `/neko/prompt.html`, `/manifest.json` (`completedDays=25`), and `nginx -t` inside local image `100-days-games:day025-smoke`.

## Remaining notes

- The custom route smoke exercises the primary controls and verifies coin/tray/gate/nudge path behavior, but does not grind long enough to naturally trigger Neko Grand Fortune. The progression path is implemented through authored commission requirements, score thresholds, requested deliveries, bell counts, misfortune/luck state, and commission advancement.
- The initial implementation subagent produced partial Day 025 app/assets while the controller was completing the run. The controller intentionally selected one final file owner and synchronized app/release/alias files before validation.
