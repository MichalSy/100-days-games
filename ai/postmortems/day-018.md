# Day 018 Postmortem — Asa Daruma Wishwheel Labyrinth

## QA summary

Day 018 was generated from the archived prompt at `prompts/day-018.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. The controller also generated and inspected Imagegen2 assets, assembled the app/release/alias output from the archived prompt, and will complete the end-to-end local validation, screenshot capture, Docker/static smoke, and deployment verification before publish.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 018 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-018.md` was written. Day 018 HTML/CSS/JS was written for `apps/day-018-asa-daruma-wishwheel-labyrinth/` from the archived Day 018 prompt and general browser/Three.js game knowledge, then copied into `release/games/018/` and `/asa/`.

## What shipped

- Public route: `/asa/`.
- Immutable archive: `release/games/018/`.
- Mode: mobile-first real `3d`, chosen after Day 017 `hybrid` and zero current 2D streak.
- Core play: tilt a raised 3D dawn shrine maze board, roll a daruma wishwheel with inertia, collect sun/moon/star ema plaques, open red/blue/gold torii gates, ring dawn bells, avoid sumi ink, use Brake/Center and Stillness Seal, and finish at the offering bowl.
- Mobile layout: portrait-first 390x844 route with compact dawn HUD, chapter card, central 3D maze board, status helper, and 44px+ tilt/action controls.
- Prompt archive: `prompts/day-018.md` copied exactly to `release/games/018/prompt.md` and `release/asa/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: lightweight WebAudio roll/ema/bell/ink/fulfillment tones initialize only after Start or another user gesture; audio fails safely if unavailable.

## Generated-image inspection notes

- `asa-daruma-source.png` / `asa-daruma.png`: readable, friendly red daruma with one painted eye, gold trim, and ema tassels. Centered crop, generous margins, strong small-size silhouette, no visible text or watermark. It uses a pale dawn background rather than true alpha transparency; acceptable because runtime maps it to a face plane/mascot texture and the 3D sphere remains the collision object. Rotation baseline is documented: the face plane always looks toward the camera so rolling motion stays readable rather than spinning the face upside down.
- `asa-shrine-terrace-source.png` / `asa-shrine-terrace.png`: portrait dawn shrine terrace with vermilion posts, bells, ema plaques, misty hills, and a large open tatami center. No characters, visible text, or watermark. The center is open and low-detail enough for the 3D board, rails, daruma, gates, plaques, ink, chapter card, helper, and controls to remain readable after dark overlay.
- `asa-icons-source.png` / `asa-icons.png`: icon sheet has distinct red/blue/gold gates, sun/moon/star ema plaques, dawn bell, offering bowl, sumi ink, seals, wish heart, and fulfillment emblem. No fake checkerboard, visible text, watermark, or edge cropping. Runtime keeps gameplay-critical controls text-labeled, using the generated sheet as visual/decorative support.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 018 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, start/gameplay, Tilt Right, Tilt Down, Brake/Center, Stillness Seal control presence, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Static checks — passed for `release/games/018/`, `/asa/`, screenshots, prompt copies, optimized assets, source assets, and non-empty game files.
- Prompt copy checks — passed: `prompts/day-018.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 018 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw Markdown dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed: desktop screenshot shows non-empty 3D maze gameplay with daruma, HUD, chapter card, controls, and readable objective; mobile 390x844 screenshot shows portrait HUD/chapter/stage/controls with no landscape gate.
- Docker/Nginx static smoke — passed for `/asa/`, `/asa/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day018-smoke`.

## Remaining notes

- The route smoke exercises primary controls and verifies the 3D scene, but does not grind long enough to trigger Asa Dawn Wish Fulfilled naturally. The progression path is implemented through authored chapter goals, score thresholds, and offering-bowl completion.
