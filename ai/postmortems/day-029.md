# Day 029 Postmortem — Hinoki Kumiko Screenwright

## QA summary

Day 029 was generated from the archived prompt at `prompts/day-029.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. The subagent created the Day 029 app shell, game logic, and Imagegen2 assets, then stopped making visible progress without a completion summary. The controller recovered by adopting the new Day 029 app/assets, completing the immutable release and public `/hinoki/` alias, rendering semantic prompt pages, updating gallery metadata, tightening mobile layout, capturing screenshots, and running independent QA/validation.

A separate read-only QA/review subagent was dispatched after controller recovery. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 029 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-029.md` was written. Day 029 HTML/CSS/JS was written for `apps/day-029-hinoki-kumiko-screenwright/` from the archived Day 029 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/029/` and `/hinoki/`.

## What shipped

- Public route: `/hinoki/`.
- Immutable archive: `release/games/029/`.
- Mode: mobile-first `2d`, chosen after Day 028 `3d` with zero latest 2D streak.
- Core play: position and rotate hinoki strips over kumiko blueprints, dry-fit/commit pieces, trim notches, clamp joints, plane burrs, swap strips, manage grain stress/craft hearts, and use Calm Measure focus to preview valid joinery.
- Mobile layout: portrait-first 390x844 route with compact HUD, commission card, large shoji workbench panel, helper/status row, and all primary controls visible in the first viewport at 44px+.
- Prompt archive: `prompts/day-029.md` copied exactly to `release/games/029/prompt.md` and `release/hinoki/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio tap, notch, clamp, plane, crack, shimmer, focus, and Master Screen cues initialize only after Start or another user gesture; visual blueprint/stress/joinery cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `hinoki-helper-source.png` / `hinoki-helper.png`: friendly carved wooden sparrow apprentice with indigo headband, brass measuring square, and chisel. The generated asset has a clean warm cypress background, no fake checkerboard, no visible text or watermark, centered crop, readable face/tool silhouette, and stable upright helper orientation. It is not true transparent alpha, but the warm background integrates cleanly with the helper card and circular in-game helper badge.
- `hinoki-workshop-source.png` / `hinoki-workshop.png`: portrait-safe woodworking atelier with shoji glow, hinoki workbench, chisels, clamps, blueprints, and wood shavings around the edges. It has no characters in the central work area, no readable text or watermark, and leaves a broad open center that keeps the canvas lattice readable on desktop and mobile.
- `hinoki-icons-source.png` / `hinoki-icons.png`: readable icon sheet with wooden lattice strip, diagonal joint, notched block, clamp, plane/chisel/tool tray, grain arrow, stress crack, sawdust, blueprint, Calm Measure compass, craft heart, symmetry mark, shoji screen, and finish seal. No fake checkerboard, no watermark/text, no edge crop issue; icons remain distinct enough for HUD/button support while controls remain text-labeled.
- `screenshot.png`: final desktop route capture shows active gameplay with HUD, commission card, shoji panel canvas, blueprint strips, helper badge, and large controls; it is readable as a kumiko woodworking game and suitable for the gallery card. Separate 390x844 mobile screenshot confirmed first-viewport usability.

## Fixes made during QA/recovery

- Recovered from a partial implementation subagent run by adopting its new Day 029 app/assets and completing controller-owned release copies, prompt pages, metadata, screenshot, and postmortem.
- Fixed a zero-byte copied `/hinoki/assets/hinoki-icons.png` by re-copying the optimized release asset.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added/copied data-URI favicons through Day 029 game and prompt pages to avoid favicon request noise.
- Tightened mobile CSS to fit the complete gameplay route into a 390x844 first viewport: compact HUD/commission/helper, 384px board, all controls visible, and 44px+ touch targets.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day029-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-wRJ1Mpxf.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 029 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/hinoki/`: menu/tutorial, Start/gameplay, Step Up/Left/Right/Down, Rotate, Dry Fit, Commit Strip, Trim Notch, Clamp Joint, Plane Burr, Swap Strip, Calm Measure, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`, board height 384.8px, controls 44px+ high, and max primary control bottom 832px.
- Static checks — passed for `release/games/029/`, `/hinoki/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-029.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed during custom route smoke: `Day 029 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed direct nested list nodes.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission/panel/helper/controls.
- Docker/Nginx static smoke — passed with local image `100-days-games:day029-smoke`: `nginx -t` succeeded; `/hinoki/`, `/hinoki/prompt.html`, and `/manifest.json` served correctly on port 18029 with `completedDays=29`. The first readiness probe saw one transient connection reset before Nginx was ready, then retry/static checks passed.

## Remaining notes

- The implementation subagent did not return a completion summary during the controller run; controller recovery is documented here.
- The custom route smoke exercises primary controls and verifies visible strip/rotation/dry-fit/commit/notch/clamp/plane/focus interaction, but does not grind long enough to naturally trigger Hinoki Master Screen. The progression path is implemented through authored commission requirements, score thresholds, stress/craft-heart state, and chapter advancement.
