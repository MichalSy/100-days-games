# Day 031 Postmortem — Botan Ikebana Balance Atelier

## QA summary

Day 031 was generated from the archived prompt at `prompts/day-031.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. While it was active, the controller saw concurrent Day 031 file writes and took ownership of the final Day 031 file set to avoid mixed HTML/CSS/JS. The controller generated Imagegen2 assets, completed the app, immutable release, public `/botan/` alias, semantic prompt pages, gallery metadata, screenshot, and validation.

A separate independent QA/review subagent was dispatched after controller recovery. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence unless QA reports a blocker.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 031 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-031.md` was written. Day 031 HTML/CSS/JS was written for `apps/day-031-botan-ikebana-balance-atelier/` from the archived Day 031 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/031/` and `/botan/`.

## What shipped

- Public route: `/botan/`.
- Immutable archive: `release/games/031/`.
- Mode: mobile-first `2d`, chosen after Day 030 `3d` with zero latest 2D streak.
- Core play: move a kenzan anchor, rotate/trim a ghost stem, preview composition quality, commit stems into a suiban, manage tilt/freshness/combo, mist thirsty blooms, support-tie heavy stems, recut one mistake, and activate Ma Focus for balance/negative-space preview.
- Mobile layout: portrait-first 390x844 route with compact HUD, commission card, 252px canvas stage, helper/status row, and all primary controls visible in the first viewport at 44px+.
- Prompt archive: `prompts/day-031.md` copied exactly to `release/games/031/prompt.md` and `release/botan/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio tap, pin, trim, mist, tie, wobble, wilt, focus, and Grand Arrangement cues initialize only after Start/user gesture; visual balance/freshness/composition cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `botan-helper-source.png` / `botan-helper.png`: friendly tanuki florist in moss apron holding a peony and bamboo measuring stick. Clean pale peony background, no fake checkerboard, no visible text or watermark, centered crop, readable face/flower/stick silhouette, stable upright helper orientation. It is not transparent alpha, but the soft background integrates cleanly in helper cards and badges.
- `botan-studio-source.png` / `botan-studio.png`: portrait-safe ikebana studio with tatami, work table, suiban, kenzan, peonies, tools, and open central table area. No characters, readable text, or watermark. The central area remains readable behind the canvas/stage overlay on desktop and mobile.
- `botan-icons-source.png` / `botan-icons.png`: readable icon sheet with peony, line grass, willow, suiban, kenzan, water ring, mist/freshness, support tie, shears, tilt warning, negative-space moon, Ma Focus emblem, studio heart, and arrangement seal. No fake checkerboard, watermark, or edge crop issue; icons are distinct enough at HUD/button size while controls remain text-labeled.
- `screenshot.png`: final desktop route capture shows HUD, commission card, active ikebana canvas with kenzan/stem/negative-space target, helper badge, and large controls; it is readable as a floral balance game and suitable for the gallery card. Separate 390x844 mobile screenshot confirmed first-viewport usability.

## Fixes made during QA/recovery

- Recovered from concurrent partial implementation output by choosing controller-owned Day 031 HTML/CSS/JS and synchronizing app/archive/alias copies.
- Regenerated `prompt.html` with flat semantic lists after the first renderer produced direct nested `ul > ul` / `ul > ol` nodes.
- Tightened mobile CSS so all controls fit in the first 390x844 viewport: 5-column control grid, 44px+ touch targets, and a 252px stage.
- Recaptured the desktop gallery screenshot at scroll top after the first capture had the page scrolled too low.
- Diagnosed `pnpm test:smoke` failure as a stale unrelated preview server on port 4173 serving `Gravity Dig`; killed that stale process and reran smoke successfully.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-CBO0DkJo.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- First `pnpm test:smoke` attempt — failed because Playwright reused a stale unrelated server on `127.0.0.1:4173` showing `Gravity Dig`. Root cause was environment/server state, not Day 031. The stale process was killed.
- Rerun `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 031 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/botan/`: menu/tutorial, Start/gameplay, Anchor Left/Up/Right/Down, Rotate, Trim, Preview, Commit Stem, Stem Rack, Mist Brush, Support Tie, Recut, Ma Focus, pause/resume, restart, prompt page, semantic prompt DOM, WebAudio after user gesture, no console errors or request failures. Mobile metrics: `bodyScrollHeight=863`, `innerHeight=844`, stage height 252px, controls bottom 837.1px, minimum target height 44px.
- Static checks — passed for `release/games/031/`, `/botan/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-031.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed after renderer fix: `Day 031 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw Markdown `<pre>` dump, no malformed `ul > p`, `ol > p`, direct nested list nodes, or corrupted inline code.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission/stage/status/controls.
- Docker/Nginx static smoke — passed with local image `100-days-games:day031-smoke`: Docker build succeeded; `nginx -t` succeeded; `/botan/`, `/botan/prompt.html`, and `/manifest.json` served correctly on port 18032 with `completedDays=31`. The first readiness probe saw one transient connection reset before Nginx was ready, then retry/static checks passed.

## Remaining notes

- The current route smoke exercises primary controls and visible feedback, but does not grind long enough to naturally trigger Botan Grand Arrangement. The progression path is implemented through authored commission requirements, score gates, tilt/freshness/hearts state, and chapter advancement.
- The helper asset uses a clean pale background rather than true transparent alpha. It is acceptable for this day because it is shown inside rounded cards/badges and was inspected at final size.
