# Day 014 Postmortem — Kiri Origami Foldwright

## QA summary

Day 014 was generated from the archived prompt at `prompts/day-014.md`. A fresh implementation subagent and a separate QA/review subagent were dispatched with the repo path, day number, archived prompt path, validation rules, and the code-isolation rule. The controller also completed the Day 014 implementation/release integration directly from the archived prompt while subagents were running, because the run needed end-to-end local validation, screenshot capture, Docker/static smoke, and deployment readiness in the controller session. During this overlap the controller observed sibling-agent writes to the new Day 014 app files and reconciled the final app/release/alias files from the archived Day 014 prompt and Day 014 assets only.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 014 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-014.md` was written. Day 014 HTML/CSS/JS was written for `apps/day-014-kiri-origami-foldwright/` from the archived Day 014 prompt and general browser/canvas game knowledge, then copied into `release/games/014/` and `/kiri/`.

## What shipped

- Public route: `/kiri/`.
- Immutable archive: `release/games/014/`.
- Mode: mobile-first meaningful `hybrid` with paper-layer fold geometry, mountain/valley fold states, route preview, crane launch path, seal collection, fog-gap hazard, tear stress, and reinforce tradeoffs.
- Core play: select a crease, apply Mountain/Valley/Unfold/Reinforce actions, preview the crane route, launch the crane to collect vermilion seals and reach the ceremony gate, manage feathers/stress/combo/time, and progress toward Kiri Thousand-Fold Blessing.
- Mobile layout: portrait-first 390x844 route with compact notebook HUD, commission card, central cutting-mat/paper playfield, readable fold lines and route preview, helper card, and 48px+ controls.
- Prompt archive: `prompts/day-014.md` copied exactly to `release/games/014/prompt.md` and `release/kiri/prompt.md`; rendered semantic prompt pages at both aliases.

## Generated-image inspection notes

- `kiri-foldwright-source.png` / `kiri-foldwright.png`: readable upright origami foldwright mascot in moss-green clothing with coral sash, white crane, and bamboo folding tool. Centered crop, stable pivot, strong small-size silhouette, no visible text or watermark. It uses a pale washi background rather than true alpha transparency; acceptable because it is menu/title art, not a moving gameplay sprite.
- `kiri-studio-source.png` / `kiri-studio.png`: portrait misty origami studio background with cedar hills, desk lamp, cutting mat, papers, and tools. Center area is open and crop-safe for phone portrait; the game overlays a dark stage and high-contrast washi sheet/fold lines so HUD, commission text, crane, seals, gate, and controls remain readable. No visible text or watermark.
- `kiri-icons-source.png` / `kiri-icons.png`: generated source was landscape despite the requested square sheet. The first optimized pass produced black/transparent letterboxing, and a crop pass cut edge icons. Final optimized runtime copy was padded on a pale green background so all icons are visible without black bars: mountain/valley fold, crane, seal, reinforce tab, tear stress, fog, gate, folding bone, and blessing seals remain distinct. No visible text, watermark, or fake checkerboard.
- `screenshot.png`: final desktop route capture shows non-empty gameplay with readable HUD, commission card, cutting-mat stage, washi sheet, fold lines, route preview, crane, seals, gate, helper, and bottom controls. A separate 390x844 mobile screenshot confirmed portrait usability and 48px+ controls.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets, inspected source files, optimized runtime copies, and copied them into app, immutable release, and `/kiri/` alias paths.
- Fixed the icon-sheet optimization after visual inspection found black/transparent letterboxing and a bad cropped version; final copy is padded on a pale background with all icons visible.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Rendered `prompt.html` as semantic HTML with headings/lists/code styling and a back-to-game link, not a raw Markdown dump.
- Captured a desktop gameplay screenshot for gallery use and a separate mobile QA screenshot.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Custom Day 014 route smoke — passed for desktop 1280x900 and mobile 390x844: menu/tutorial, start/gameplay, crease selection, Mountain, Valley, Unfold, Reinforce control presence, Launch Crane, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Static checks — passed for `release/games/014/`, `/kiri/`, screenshots, prompt copies, optimized assets, source assets, and non-empty game files.
- Prompt copy checks — passed: `prompts/day-014.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 014 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists, no body-level raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Docker/Nginx static smoke — passed for `/kiri/`, `/kiri/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day014-smoke`.

## Remaining notes

- The route smoke exercises the primary controls and verifies the hybrid fold/crane scene, but does not grind long enough to trigger Kiri Thousand-Fold Blessing naturally. The progression path is implemented through chapter goals and score/stress thresholds.
- The generated icon sheet needed post-processing because Imagegen2 returned a landscape composition. Future prompts may ask for “square grid with generous edge margin and no edge-cropped icons” even more explicitly.
