# Day 032 Postmortem — Onsen Steamline Bathkeeper

## QA summary

Day 032 was generated from the archived prompt at `prompts/day-032.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation/publish rules, and the code-isolation rule. The subagent created the initial Day 032 app shell and vendored Three.js files, but progress stalled before a complete playable release. The controller recovered by taking ownership of the Day 032 file set, generating Imagegen2 assets, completing the app, immutable release, public `/onsen/` alias, semantic prompt pages, gallery metadata, screenshot, and validation.

A separate independent QA/review subagent was dispatched after controller recovery. The controller route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence unless QA reports a blocker.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 032 implementation/recovery/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-032.md` was written. Day 032 HTML/CSS/JS was written for `apps/day-032-onsen-steamline-bathkeeper/` from the archived Day 032 prompt and general browser/Three.js/WebGL/WebAudio game knowledge, then copied into `release/games/032/` and `/onsen/`.

## What shipped

- Public route: `/onsen/`.
- Immutable archive: `release/games/032/`.
- Mode: mobile-first real `3d`, chosen after Day 031 `2d` with a latest 2D streak of one.
- Core play: select near/mid/far bathhouse lanes, rotate copper valves through cool/neutral/hot/outlet states, tune pool temperatures, vent pressure, use Bamboo Spout and Stir Pool tools, satisfy macaque comfort bubbles, preserve mineral glow, and activate Yuge Focus for visible prediction/slow-time overlays.
- Mobile layout: portrait-first 390x844 route with compact HUD, commission card, 414px 3D stage, helper/status row, and all primary controls visible in the first viewport at 44px+.
- Prompt archive: `prompts/day-032.md` copied exactly to `release/games/032/prompt.md` and `release/onsen/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio tap, valve, steam, splash, stir, warning, macaque, focus, and Grand Yuge cues initialize only after Start/user gesture; visual temperature/pressure/steam cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `onsen-helper-source.png` / `onsen-helper.png`: friendly upright snow macaque bathhouse helper in indigo onsen robe/headband, holding a bamboo ladle and copper valve key. Clean pale mist background, no fake checkerboard, no visible text or watermark, centered crop, readable face/ladle/key silhouette, and stable upright helper orientation. It is not true transparent alpha, but the soft background integrates cleanly in cards/badges.
- `onsen-bathhouse-source.png` / `onsen-bathhouse.png`: portrait-safe onsen bathhouse with cedar/stone pools, copper pipes, bamboo spouts, lanterns, steam, and a broad open central floor area. No characters, readable text, or watermark. The central playable area remains readable behind the 3D bathhouse board on desktop and mobile.
- `onsen-icons-source.png` / `onsen-icons.png`: readable icon sheet with cedar pool, stone pool, copper valve wheel, hot steam, cool mist, bamboo spout, vent chimney, pressure gauge, thermometer, condensation/droplet, macaque face, Yuge Focus steam emblem, comfort heart, mineral crystal, and bathhouse seal. No fake checkerboard, watermark, edge crop, or confusing text; icons are distinct enough at HUD/button size while controls remain text-labeled.
- `screenshot.png`: final desktop route capture shows active gameplay with HUD, commission card, 3D pools/valves/ducts/steam, Yuge Focus overlay, helper/status, and large controls; it is readable as a 3D onsen thermal-routing game and suitable for the gallery card. Separate 390x844 mobile screenshot confirmed first-viewport usability.

## Fixes made during QA/recovery

- Recovered from a partial implementation subagent output by adopting the Day 032 shell/Three.js files and completing controller-owned CSS/JS/assets/release/alias/metadata.
- Generated and optimized Imagegen2 helper, background, and icon assets; archived source files under `release/games/032/assets/source/` and copied optimized playable assets to app, immutable release, and public alias.
- Rendered `prompt.html` with semantic headings/lists/code, title/h1, and back-to-game link; static/browser probes confirmed no raw Markdown `<pre>` dump, malformed list nodes, or `<strong>` inside `<code>`.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Tuned mobile CSS to fit the complete gameplay route into a 390x844 first viewport: compact HUD/commission/helper, 414px stage, all controls visible, and 46px+ touch targets.
- Adjusted Yuge Focus so the control has a visible practice preview before full charge, while full charge still provides the longer slow-time/prediction effect.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day032-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html`, `release/assets/index-DFXTTwkp.css`, and `release/assets/index-CwJlJVbD.js`; release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 032 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/onsen/`: menu/tutorial, Start/gameplay, Lane −/+, Valve −/+, Vent Chimney, Bamboo Spout, Stir Pool, Yuge Focus visible overlay/effect, pause/resume, restart, prompt page, semantic prompt DOM, WebAudio after user gesture, no console errors or request failures. Mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`, stage height `414.0625px`, minimum target height `46px`, controls bottom `827px`.
- Static checks — passed for `release/games/032/`, `/onsen/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, vendored Three.js files, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-032.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 032 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw Markdown `<pre>` dump, no malformed `ul > p`, `ol > p`, direct nested list nodes, or corrupted inline code.
- Screenshot/vision QA — passed: desktop screenshot is non-empty, active, readable, visually polished, and suitable as gallery card art; mobile screenshot is portrait-friendly with readable HUD/commission/stage/status/controls.
- Docker/Nginx static smoke — passed with local image `100-days-games:day032-smoke`: Docker build succeeded; `nginx -t` succeeded; `/onsen/`, `/onsen/prompt.html`, and `/manifest.json` served correctly on port 18033 with `completedDays=32`. The first readiness probe saw one transient connection reset before Nginx was ready, then retry/static checks passed.

## Remaining notes

- The route smoke exercises primary controls and visible feedback, but does not grind long enough to naturally trigger Onsen Grand Yuge. The progression path is implemented through authored commission targets, score thresholds, pressure/comfort-heart state, and chapter advancement.
- The helper asset uses a clean pale background rather than true transparent alpha. It is acceptable for this day because it is shown inside rounded cards/badges and was inspected at final size.
