# Day 026 Postmortem — Natsu Hanabi Sky Painter

## QA summary

Day 026 was generated from the archived prompt at `prompts/day-026.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. During the run, the controller detected concurrent Day 026 writes from that subagent and intentionally selected the controller-owned implementation as the final file owner, then synchronized final app files into `apps/day-026-natsu-hanabi-sky-painter/`, `release/games/026/`, and `/natsu/` before validation.

A separate QA/review subagent was dispatched after implementation. The controller custom route smoke, screenshot/vision review, static checks, Docker/static smoke, and full local validation below are the authoritative pre-push evidence.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 026 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, recent prompts/postmortems, and screenshots for variety planning before `prompts/day-026.md` was written. Day 026 HTML/CSS/JS was written for `apps/day-026-natsu-hanabi-sky-painter/` from the archived Day 026 prompt and general browser/Three.js/WebAudio game knowledge, then copied into `release/games/026/` and `/natsu/`.

## What shipped

- Public route: `/natsu/`.
- Immutable archive: `release/games/026/`.
- Mode: mobile-first real `3d`, chosen after Day 025 `2d` with only a one-game 2D streak.
- Core play: aim a lacquer launch tube, select shell color, cycle fuse timing, charge and launch shells through 3D sky rings, burst at target altitude/depth, clear smoke with Wind Fan, use Slow Fuse focus, and progress toward Natsu Grand Hanabi.
- Mobile layout: portrait-first 390x844 route with compact HUD, mission card, 3D sky stage, helper/status text, and 52px controls inside the first viewport (`bodyScrollHeight=844`, `innerHeight=844`).
- Prompt archive: `prompts/day-026.md` copied exactly to `release/games/026/prompt.md` and `release/natsu/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio launch, ring, burst, fan, and focus cues initialize only after Start or another user gesture; visual arc/burst cues remain playable if muted/unsupported.

## Generated-image inspection notes

- `natsu-helper-source.png` / `natsu-helper.png`: friendly tanuki pyrotechnician helper with fan and golden firework shell. The generated asset has a clean dark indigo festival background, no fake checkerboard, no visible text or watermark, centered crop, strong readable silhouette, stable upright helper orientation, and suits menu/helper use. It is not true transparent alpha, but the dark background integrates cleanly in the runtime helper card.
- `natsu-festival-source.png` / `natsu-festival.png`: portrait summer riverbank/fireworks launch-deck background with open central sky, lanterns and launch racks at the sides/bottom, no readable text or watermark, and strong crop safety. After QA, the Three.js scene was made transparent so the background remains visible behind the 3D rings/shell targets on desktop and mobile.
- `natsu-icons-source.png` / `natsu-icons.png`: readable icon sheet with gold/cyan/magenta/white firework shell motifs, sky ring, smoke cloud, lantern, fan, fuse dial, launch tube, crowd gauge, and Grand Hanabi crest. No fake checkerboard, watermark, or edge crop issue. Runtime controls remain text-labeled while the sheet supports the mission card visual language.
- `screenshot.png`: final desktop route capture shows active gameplay with readable HUD, mission card, 3D sky stage over the festival background, launch/ring targets, tanuki helper, status helper, and controls. Separate 390x844 mobile screenshot confirmed first-viewport usability and all controls visible.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual generated files with vision before publish.
- Optimized generated assets and archived sources only under `release/games/026/assets/source/`; copied playable assets into app, immutable release, and `/natsu/` alias paths.
- Reconciled concurrent subagent/controller writes by choosing controller-owned Day 026 app files as final and copying them to both served release locations.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Fixed route-smoke script strict locator issue and verified Start/Pause/Resume/Restart flow.
- Made the Three.js renderer transparent so the generated festival background is visible behind the 3D play objects instead of a flat dark stage.
- Removed the decorative icon-sheet background image from the mobile mission card to keep the title/objective readable at 390x844.
- Captured a desktop gameplay screenshot for gallery use and mobile QA screenshots under `/tmp/day026-smoke/`.

## Validation run

- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; Vite built `release/index.html` and bundle `release/assets/index-C7VBTN_D.js`, release permissions normalized.
- `pnpm release:validate` — passed: `release validation passed`.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed: `immutable guard passed`.
- Custom Day 026 route smoke — passed for desktop 1280x900 and mobile 390x844 against `/natsu/`: menu/tutorial, start/gameplay, Aim Up/Left/Right, Shell Color, Fuse Timing, Charge Launch, Wind Fan, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures. Mobile metrics: `bodyScrollHeight=844`, `innerHeight=844`, stage height 253px, controls 52px high and bottom at 839px.
- Static checks — passed for `release/games/026/`, `/natsu/`, screenshots, prompt copies, semantic prompt HTML, optimized assets, source assets, `src/data/games.ts`, and `release/manifest.json`.
- Prompt copy checks — passed: `prompts/day-026.md` matches both release prompt markdown files exactly.
- Prompt HTML checks — passed: `Day 026 Game Generation Prompt` title/h1, back-to-game link, semantic headings/lists/code, no raw `<pre>` dump, and no malformed `ul > p` / `ol > p` nodes.
- Screenshot/vision QA — passed after the transparent-stage/mobile-mission fixes: desktop screenshot is non-empty, active, readable, visually polished enough for gallery; mobile screenshot is portrait-friendly with readable HUD/mission/stage/helper/controls.
- Docker/Nginx static smoke — passed for `/natsu/`, `/natsu/prompt.html`, `/manifest.json` (`completedDays=26`), and `nginx -t` inside local image `100-days-games:day026-smoke` on port 18026. Initial attempt on host port 18080 hit an unrelated existing mobile-log-sink service returning 401, so the Docker smoke was rerun on free port 18026.

## Remaining notes

- The custom route smoke exercises primary controls and verifies 3D route interaction, but does not grind long enough to naturally trigger Natsu Grand Hanabi. The progression path is implemented through authored commission requirements, ring count, shell order, smoke/safety state, score thresholds, and chapter advancement.
- The first implementation subagent created partial Day 026 files while the controller was completing the run. The controller intentionally selected one final file owner and synchronized app/release/alias files before validation.
