# Day 019 Postmortem — Matsuri Taiko Lanternline Maestro

## QA summary

Day 019 was generated from the archived prompt at `prompts/day-019.md`. A fresh implementation subagent was dispatched with only the repo path, day number, archived prompt path, validation rules, and code-isolation rule. Because the controller needed to keep the scheduled run moving and no subagent summary had returned before recovery work began, the controller completed and reconciled the final app/release/alias output directly from the archived Day 019 prompt, then ran local browser/mobile smoke, screenshot capture, static checks, and validation.

The Hermes browser tool path was not required for local route QA; the custom route smoke used the repo Playwright stack with `/usr/bin/google-chrome` against a static `release/` server.

## Code-isolation confirmation

No previous game implementation source was intentionally opened, inspected, copied, diffed, grepped, or adapted during the Day 019 implementation/QA work. Previous days were used only through `src/data/games.ts`, `release/manifest.json`, prompts, postmortems, and screenshots for variety planning before `prompts/day-019.md` was written. Day 019 HTML/CSS/JS was written for `apps/day-019-matsuri-taiko-lanternline-maestro/` from the archived Day 019 prompt and general browser/canvas/WebAudio game knowledge, then copied into `release/games/019/` and `/matsuri/`.

## What shipped

- Public route: `/matsuri/`.
- Immutable archive: `release/games/019/`.
- Mode: mobile-first `2d`, chosen after Day 018 `3d` and zero current 2D streak.
- Core play: hit large Don/Ka/Hi/Ya taiko pads as cue rings cross the hit band, open lantern gates, route active carriers left/right, repeat call-and-response phrases, build crowd energy, charge Festival Focus, and push toward Matsuri Grand Encore.
- Mobile layout: portrait-first 390x844 route with compact festival HUD, performance card, central rhythm-lane playfield, timing helper, and 64px+ drum pads plus large route/focus/pause/restart controls.
- Prompt archive: `prompts/day-019.md` copied exactly to `release/games/019/prompt.md` and `release/matsuri/prompt.md`; rendered semantic prompt pages at both aliases.
- Audio feel: WebAudio Don/Ka/Hi/Ya tones, miss/Great variations, call-echo preview, Focus and Grand Encore cues initialize only after Start or another user gesture; visual timing remains playable if muted/unsupported.

## Generated-image inspection notes

- `matsuri-conductor-source.png` / `matsuri-conductor.png`: readable, friendly kitsune/fox-mask taiko conductor mascot with indigo happi coat, red headband, two drumsticks, and golden lantern tassel. Centered crop, generous margins, strong small-size silhouette, no visible text or watermark. It uses a plain white/pale background rather than true alpha transparency; acceptable because it is helper/menu art, not a collision sprite. Drumstick pose is symmetric enough that it does not imply a wrong pad direction.
- `matsuri-street-source.png` / `matsuri-street.png`: portrait blue-hour festival street with magenta/gold lantern rows, yatai stalls at the sides, fireworks, confetti, and a dark open center road. No visible text, characters, or watermark. The center remains readable after the game overlay and supports four cue lanes, hit band, carrier chips, helper, and controls at desktop and 390x844 mobile sizes.
- `matsuri-icons-source.png` / `matsuri-icons.png`: icon sheet has distinct taiko drum, Don/Ka-style hits, clap/chant-like icons, lantern carriers, gate, fox mask, fan, firework, crowd heart, Focus seal, and Grand Encore emblem. No fake checkerboard, visible text, watermark, or edge cropping. Critical gameplay controls remain text-labeled, using the icon sheet as visual/decorative support.

## Fixes made during QA/recovery

- Generated three Imagegen2 assets and inspected actual source/runtime files with vision before publish.
- Optimized/copied playable assets into app, immutable release, and `/matsuri/` alias paths; source assets are archived only under `release/games/019/assets/source/`.
- Rendered `prompt.html` as semantic HTML with title/h1, headings/lists/code styling, and a back-to-game link, not a raw Markdown `<pre>` dump.
- Added data-URI favicons to game and prompt pages to avoid favicon request noise.
- Wrote a custom Playwright route smoke and corrected its Start-button selector so it did not accidentally hit Restart before the title overlay was dismissed.
- Captured a desktop gameplay screenshot for gallery use and a separate mobile QA screenshot at `/tmp/day019-smoke/mobile-gameplay.png`.

## Validation run

- `node /tmp/day019-static-check.mjs` — passed after screenshot capture: required Day 019 app/release/alias files, prompt copies, semantic prompt HTML, assets, `src/data/games.ts`, and `release/manifest.json` checks passed.
- Custom Day 019 route smoke — passed for desktop 1280x900 and mobile 390x844 using `/usr/bin/google-chrome`: menu/tutorial, Start, gameplay, Don/Ka/Hi/Ya controls, lane left/right, Festival Focus control presence, pause/resume, restart, prompt page, semantic prompt DOM, no console errors or request failures.
- Screenshot/vision QA — passed: desktop screenshot shows non-empty matsuri rhythm gameplay with HUD, performance card, cue lanes, hit band, active carriers, mascot, and large controls; mobile screenshot shows portrait HUD/performance card/playfield/pads/route/focus/system controls without a landscape gate.
- Generated-image QA — passed for optimized mascot, background, and icon sheet as documented above.
- `pnpm typecheck` — passed.
- `pnpm test` — passed: 1 test file / 4 tests.
- `pnpm build` — passed; gallery bundle rebuilt and release permissions normalized.
- `pnpm release:validate` — passed.
- `pnpm test:smoke` — passed: 4 Playwright gallery tests across desktop/mobile.
- `pnpm test:immutable -- --base origin/main` — passed.
- Docker/Nginx static smoke — passed for `/matsuri/`, `/matsuri/prompt.html`, `/manifest.json`, and `nginx -t` inside local image `100-days-games:day019-smoke`.

## Remaining notes

- The route smoke exercises the core rhythm controls and visual/audio initialization path but does not grind long enough to naturally trigger Matsuri Grand Encore. The progression path is implemented through authored act thresholds, call phrases, Focus charge, and score gates.
- Because this day is sound-themed, future rhythm prompts should keep the explicit “visual timing remains playable muted” acceptance criterion; it made smoke testing and accessibility clearer.
